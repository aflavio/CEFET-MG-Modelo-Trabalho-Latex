\begin{python}
def MaxMin1(A):
	Max = A[0]
	Min = A[0]
	j = int()
	
	for i in range(1, len(A)):
	
		j += 1 % Contador 
		if A[i] > Max:
		Max = A[i]
		
		j += 1 % Contador 
		if A[i] < Min:
		Min = A[i]
	
	return j 

\end{python}
		